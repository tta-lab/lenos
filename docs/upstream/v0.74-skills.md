# Skills (Medium priority)

> ~16 commits. We kept skills but our implementation differs from upstream.

---

## 1. Skill picker / display (MERGE)

### `55721653` — feat(skills): add descriptions to skill picker and use attachments
Shows skill description in the picker list. Supports attachments.

**Files changed:** skill picker dialog
**Check:** Our `skill list` / picker — does it show descriptions? We have
`skill get` for full view but picker may only show name.

---

### `8e7451fc` — fix(ui): show custom skills reliably on startup
Race condition: skills not visible until TUI refresh.
Package-level cache for discovery states. Adds `~/.agents/skills/` and
`~/.claude/skills/` as global scan paths.

**Files changed:** skill discovery (`discover.go`), coordinator
**Check:** Our skill discovery — any startup race? We already scan
`~/.agents/skills/` per `cmd-skill.md`. Verify cache implementation.

---

### `0e039d00` — fix(ui): prevent duplicate custom skills from rendering
Dedup logic in skill rendering.

**Files changed:** skill picker/list renderer
**Check:** Our skill list — any dedup? If a skill appears in both builtin
and custom paths, does it show twice?

---

### `7a6149df` — fix(ui): don't show disabled skills
Skills with `disabled: true` in frontmatter are hidden.

**Files changed:** skill list renderer
**Check:** Our SKILL.md frontmatter parser — does it respect `disabled`?

---

## 2. Discovery / coordinator (MERGE with care)

### `6716ef09` — feat(skills): add support for user invocable skills
Skills with `invocable: true` can be triggered by user from picker.
Agent doesn't auto-load them.

**Files changed:** skill model (add `Invocable` field), coordinator
**Check:** Do we need this concept? Our skills are agent-loaded.

---

### `b8989b96` — refactor(skills): make coordinator the sole skill discovery publisher
Eliminates double-publish: `DiscoverWithStates` emits, then coordinator
emits again. Now coordinator is the only publisher.

**Files changed:** `internal/agent/coordinator.go`, skill discovery
**Check:** Our coordinator's skill initialization — is there a double
publish? Check `discoverSkills` calls.

---

### `2da70c19` — feat(skills): log skill activation to help diagnose load failures
Add debug log when each skill is activated.

**Files changed:** coordinator or discovery
**Check:** One `slog.Debug` call. Trivial.

---

## 3. Prompts (MERGE)

### `e64db33e` — feat(prompt): require loading appropriate skills before acting
Models frequently skip viewing SKILL.md and infer behavior from description.
Makes loading mandatory in `<critical_rules>`. Our coder.md already does
this partially — check if our prompt wording could be improved.

**Files changed:** `internal/agent/templates/coder.md`
**Check:** Our `coder.md` — does it require skill loading before acting?

---

## 4. Misc (MERGE)

### `7437d2b8` — fix: silence unless warning about non-existent skill paths
When a configured skill path doesn't exist, suppress the stderr warning.

**Files changed:** discovery file walk
**Check:** Our discovery — does it warn on missing paths?

---

### `0a86d5f7` — refactor(skills): strip implementation hints from crush-owned descriptions
Removes phrases like "using go doc" from builtin descriptions.
Descriptions should say *when* to use, not *how*.

**Files changed:** builtin skill descriptions
**Check:** Our builtin skill descriptions — same clean-up possible.

---

## 5. Skip (server-specific)

- `d0254039` — fix(server): display available skills in client → SKIP
- `b9fdf72e` — chore(hooks,skills): update crush-hooks skill → SKIP
- `a7546adc` — chore(tests): regression test for disabled skills → SKIP
- `81daa266` — fix(styles): use semantic names in styles + drop deadcode → SKIP

---

## Merge plan

1. `8e7451fc` — skill discovery cache + global paths
2. `0e039d00` — dedup rendering
3. `7a6149df` — disabled skill filter
4. `2da70c19` — activation logging
5. `e64db33e` — prompt improvement
6. `7437d2b8` — silence missing path warnings

Total diff: ~100 lines across 3-4 files.
