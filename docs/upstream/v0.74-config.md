# Config/Service (High priority)

> ~20 commits after filtering. Shared config layer — directly mergeable.

---

## 1. Config fixes (MERGE ALL)

### `c853a939` — fix(config): always resolve the data directory to an absolute path
The data directory is now always resolved to an absolute path before use,
fixing bugs where relative data dirs caused problems in subprocesses and
transcript writing.

**Files changed:** `internal/config/config.go` (the `DataDirectory()` getter or equivalent)
**Check:** In our config, find where we set `DataDirectory` from CLI flag or env var.
Ensure the path is resolved to absolute before use. Likely in `root.go` or `config/load.go`.

---

### `61a9fced` — fix(config): individual errors on json parse
Instead of failing silently on the first parse error, report each individual
parse error with its field context.

**Files changed:** `internal/config/load.go`
**Check:** Our config loading path (`config.json` parse). Look for `json.Unmarshal`
or `json.Decoder` — add per-field error logging.

---

### `da262be6` — fix(schema): fix schema descriptions being cut off
Trivial fix for JSON schema generation where long descriptions were truncated.

**Files changed:** `internal/config/schema.go` or config struct tags
**Check:** We likely don't generate schema — SKIP if we don't.

---

## 2. Path discovery (MERGE with rename)

### `e2e0bc09` — fix(config): scope .crush discovery to the current repo
The upward walk for `.crush` (our `.lenos`) had no boundary — could bleed
across worktrees. Stops at git working tree root.

**Files changed:** `internal/config/load.go` — the discovery/walk logic (likely `fsext.LookupClosest`)
**Check:** Our `.lenos` discovery uses `fsext.LookupClosest` which walks UP
from cwd. Check if `LookupClosest` has a boundary or if it walks all the way
to `/`. If no boundary, port the boundary logic (commit `79b2d619`).

---

### `e1123687` — fix(config): scope crush.json discovery to the current repo
Same boundary for the JSON config file discovery (`.crush.json`/`config.json`).

**Files changed:** same as above
**Check:** Where do we look for `config.json`? Is it scoped or global?

---

### `79b2d619` — feat(fsext): stop upward lookup at a boundary directory
The underlying implementation: `fsext.LookupClosest` now accepts a boundary
— stops at git worktree root or cwd if no git worktree.

**Files changed:** `internal/fsext/fsext.go`
**Check:** Does our `fsext.LookupClosest` accept a boundary? If not, we need
this commit's interface change before the above two work.

---

### `a7932c56` — test(config): tests for the data directory paths
Tests only.

---

## 3. Model fallback (MERGE)

### `cc7d651a` — fix(config): use large model for small if not configured
When `SmallModel` is not explicitly configured, fall back to `LargeModel`
instead of empty string.

**Files changed:** `internal/config/provider.go` or `config.go` — model resolution
**Check:** Our `SetActiveModel` / `UpdatePreferredModel` path. When
`Models[Small]` is empty, do we crash or fall back to `Models[Large]`?
We may already handle this in `applyEphemeralModelOverride` (commit f27639b3
added active-tier dispatch). Verify the fallback exists.

---

## 4. OAuth token management (MERGE with care)

### `99bc5ce8` — fix(config): check config file for newer token before OAuth refresh
Before refreshing an OAuth token, read config file from disk. If another
session already refreshed it, use disk token and skip external refresh.
Prevents unnecessary 401s when multiple sessions are running.

**Files changed:** `internal/config/store.go` — the OAuth refresh path
**Check:** Our OAuth refresh in `internal/oauth/` — do we check disk first?
Applicable to any OAuth provider (codex, hyper, copilot).

---

### `8bc4a752` — fix(config): atomically update multiple fields during oauth
When OAuth updates multiple config fields (token, expiry, refresh token),
do it atomically instead of one field at a time.

**Files changed:** `internal/config/store.go` — `SetConfigFields()` or equivalent
**Check:** Our config store — do we have atomic multi-field update? If not,
port the pattern.

---

## 5. Shell expansion (MERGE with care)

### `f9134777` — config: remove unused environment variable resolver
Deleted dead code (hand-rolled `$VARNAME-only` resolver). Not affect us.

---

### `f23e9982` — config: fail provider header expansion loudly and drop empty values
When a command inside a header field fails, stop the provider from loading
with a clear error. Empty-valued headers are dropped from the request.

**Files changed:** `internal/config/provider.go` — header expansion
**Check:** Do we expand env vars in provider config (api_key, endpoint,
extra_headers)? We likely do via `os.Expand` or similar. The pattern of
failing loudly on expansion errors is worth adopting. Empty header drop
is also good practice.

---

## 6. Provider-specific (SKIP unless we have that provider)

- `57ae2b44` — fix(bedrock): improve detection of pre-existing aws credentials
- `36f0976c` — fix(bedrock): load aws credentials for bedrock europe
- `ae20f1f6` — feat: add aws bedrock europe (#3016)
- `74b84f69` — fix(bedrock): enforce `us-east-1` as region for bedrock
- `4882cbc0` — feat: add alibaba (singapore) provider (#2949)
- `841eec8f` — fix(models): fix sorting of hyper
- `f34f890e` — fix(deepseek): fix 400 bad request for deepseek provider
- `c7368b7c` — fix: fix thinking on/off toggle for certain openai-compat providers
- `c8379c30` — fix(auth): add better atomic refresh for hyper

**Recommendation: SKIP unless we add that provider later.** Each is
a provider-specific fix that doesn't change the config interface.

---

## 7. Notifications/Skills config (LOW priority)

- `3fa7d3f2` — feat(notifications): migrate disabled and add picker
- `e7f5c685` — feat(notifications): add configurable backend and bell support
- `8e7451fc` — fix(ui): show custom skills reliably on startup (mentioned
  in skills doc too)

**Recommendation: DEFER** — notification config doesn't affect core.

---

## Merge plan (config)

1. `c853a939` — resolve data dir to absolute → easy, one-line check
2. `cc7d651a` — model fallback → verify or add fallback logic
3. `61a9fced` — individual json parse errors → add per-field logging
4. `e2e0bc09` + `e1123687` + `79b2d619` — path discovery boundaries →
   check `fsext.LookupClosest` interface, port boundary feature
5. `99bc5ce8` — OAuth disk check → add disk-first refresh
6. `8bc4a752` — atomic config write → add SetConfigFields
7. `f23e9982` — header expansion hardening → add error messages

Total diff: ~200 lines across 4-6 files.
