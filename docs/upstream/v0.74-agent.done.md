# Agent/Loop (Medium priority)

> ~25 commits after excluding hyper/oauth/copilot/tools-only changes.
> Agent loop is our biggest deviation (bash-first vs tool-call) but
> several fixes are protocol-independent.

---

## Token/usage accounting (MERGE ALL)

These form a chain: estimate → correct → harden → clear → mark.

### `6ed8852b` — fix(agent): estimate missing streamed usage
Adds fallback token estimator for streamed steps returning zero usage.
Updates prompt/completion counters but never contributes cost.
Provider-reported usage still preserves normal cost accounting.

**Files changed:** `internal/agent/agent_run.go`, `internal/agent/event.go`
**Check:** Our `agent_run.go` also tracks usage. Need to find the
zero-check branch and add estimator.

### `2e9c6505` — fix(agent): correct fallback usage accounting
Follow-up fix: edge case where estimator double-counts.

### `74e6e378` — fix(agent): harden fallback usage accounting
Boundary checks on the fallback path.

### `83d2abbf` — fix(agent): clear stale summary token counts
After summarization, old token counts should be zeroed to avoid
confusing the context-pressure display.

### `2736e487` — fix(ui): mark estimated context usage
UI shows estimated usage differently from provider-reported (grey out
estimated numbers).

**Files changed:** `internal/agent/agent_run.go`, `internal/agent/event.go`,
`internal/agent/agent_session.go`, UI files
**Check:** Our UI may need similar estimated/provided distinction.

---

## Race/correctness fixes (MERGE ALL)

### `9d346688` — fix(agent): release activeRequests before publishing TypeAgentFinished
Bug: TUI stays stuck on spinner after turn finishes because
`IsSessionBusy()` polls activeRequests which hasn't been released yet.
Reorder so cleanup runs before notification.

**Files changed:** `internal/agent/agent_run.go`
**Check:** Do we have `activeRequests`? Our loop structure is different
(bash-first, not tool-call). Find the equivalent: where do we release
session lock after a turn? If we have a completion notification, ensure
it fires after the session state is cleaned up.

### `61f49b23` — fix(agent): drain queued messages after manual session summarize
Manual summarize leaves queued messages unprocessed.

**Files changed:** `internal/agent/agent_session.go`
**Check:** Our summarize path. Same issue likely exists.

### `6bdd233b` — fix(ui): display error on summarization instead of leaving spinning
**Check:** Our summarize error handling — does it show the error or
leave the spinner?

### `a4020df6` — fix(summarize): reauthenticate oauth tokens when used to summarize
OAuth token may expire between regular turns; summarize needs its own
reauth path.

**Files changed:** `internal/agent/coordinator.go`
**Check:** Our coordinator's summarize path — does it refresh tokens?

---

## Env/shell hardening (MERGE)

### `c2be8cbf` — fix: set non-interactive env vars in shell to prevent editor hangs
Sets `TERM=xterm-256color`, `EDITOR=false`, `VISUAL=false`, `PAGER=cat`,
`GIT_PAGER=cat`, `JJ_EDITOR=false`, `JJ_PAGER=cat` for **all** shell
executions. Prevents commands from spawning editors or pagers and
hanging on a nonexistent TTY.

**Files changed:** `internal/agent/exec.go` (LocalRunner.Run)
**Check:** Directly applicable — our `LocalRunner` or `exec.Command`
path. We set some env but not all of these. Add to the env map or
in the subprocess setup.

---

## Reasoning/model fix (MERGE)

### `2faa467a` — fix: fix sometimes sending reasoning effort when it shouldn't
Sometimes `model.ModelCfg.ReasoningEffort` (e.g. "high") is set on a model
that doesn't support reasoning effort levels. Check `canReason` before
sending. Otherwise set only `thinking` (if supported).

**Files changed:** `internal/agent/agent_run.go`, `internal/config/provider.go`
**Check:** Our provider config — do we have the same leak? Find where
`ProviderOptions.ReasoningEffort` is set from `ModelCfg` and add the
capability check.

### `a924ca1c` — fix(tui): show initialization mark errors in status footer
**Check:** Our TUI initialization — does it show errors?

---

## Cost/misc (MERGE)

### `5816fada` — fix(agent): support flat_rate cost handling (#2116)
**Files changed:** `internal/agent/event.go`, usage tracking
**Check:** Our cost tracking structure — do we handle flat_rate case?

### `8cd4786c` — refactor(coordinator): extract token refresh helpers
Extract `refreshTokenIfExpired` and `retryAfterUnauthorized` from both
`Run` and `Summarize`.

**Files changed:** `internal/agent/coordinator.go`
**Check:** Same duplication exists in our coordinator.

### `549f7172` — fix(agent): implement OnRetry logging with structured retry fields
**Check:** Our retry path — does it log structured fields?

---

## Skip (tool-call specific)

- `1528c82f` — inject synthetic tool_result for orphaned tool_use → SKIP
- `c41a1a0e` — validate tool call/results → SKIP (title tag stripping
  part maybe useful)
- `efe42e80` — prevent session corruption from malformed image data → SKIP
- `11eabdfc` — surface errors in subagents → SKIP (no subagents)
- `1528c82f` — orphaned tool_use → SKIP
- `6d95ecc5` — skip image attachments when model doesn't support → SKIP
  (we don't support image attachments)

---

## Merge plan

1. Port `c2be8cbf` — easy: add env vars in `LocalRunner.Run`
2. Port token accounting chain (5 commits) — find usage tracking in
   `agent_run.go`/`event.go`
3. Port `9d346688` — reorder cleanup before notification in `agent_run.go`
4. Port `2faa467a` — add capability check before setting reasoning_effort
5. Port `8cd4786c` — extract token refresh helpers
6. Port `61f49b23` — drain queued messages after summarize
7. Port `5816fada` — flat_rate cost handling

Total diff: ~150 lines across 5+ files.
