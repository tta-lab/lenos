# Pubsub (Medium priority) ✅ DONE

> 9 commits in v0.55.0→v0.74.1. 3 merged, 4 skipped (server-only), 2 unverified.

---

## 1. Buffer fix

### `0585f498` — fix(pubsub): raise default per-subscriber buffer (64→4096)
✅ **MERGED** (PR #120)

Audited upstream: buffer const raised from 64→4096, dead `maxEvents` field
removed from struct and `NewBrokerWithOptions`. Upstream diff applies cleanly.

### `abeaff09` — fix(pubsub): respect channelBufferSize parameter in Subscribe
✅ **MERGED** (PR #120)

Audited upstream: `NewBrokerWithOptions` now stores `channelBufferSize` in
struct; `Subscribe` uses `b.channelBufferSize` instead of hardcoded const.
Applied identically.

---

## 2. Diagnostic logging

### `b223e24e` — feat(logs): add a log line for dropped events
✅ **MERGED** (PR #120)

Audited upstream: added `slog.Warn("Pubsub buffer full; dropping event")`
in the non-blocking send's `default` case. Our version uses the message
`"pubsub: event dropped, subscriber buffer full"` with `"type"` key (matches
our existing log style in `app.go`).

---

## 3. Server-specific

- `d9acf860` — feat(server): broadcast config changes to connected clients
- `d0254039` — fix(server): display available skills in client
- `0bbc8a87` — fix(ui): restore pills to-do in client/server mode
- `e14d47a8` — fix(noninteractive): crush run reliability (server mode)

❌ **SKIPPED** — client-server architecture removed from Lenos. These commits
modify files (`internal/server/`, `internal/client/`) that don't exist in our
tree.

---

## Merge plan (complete)

All applicable upstream commits ported. PR #120 contains both the
three pubsub fixes and the WORKFLOW.md→skill conversion with §1.3
audit step.
