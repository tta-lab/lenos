# Other (Low priority)

> Notifications, fsext, prompts, provider-specific.

---

## 1. Notifications (~5 commits)

### Relevant

- `3fa7d3f2` — feat(notifications): migrate disabled and add picker
- `e7f5c685` — feat(notifications): add configurable backend and bell support
- `6da5c959` — feat: support notifications for ssh terminal

**Our system:** `internal/agent/notify/` has NativeBackend with desktop
notifications. No configurable backend picker, no bell, no SSH support.

**Recommendation: DEFER** — nice-to-have but not critical. Our notification
system is simple and works. Upstream's approach uses different interface.

---

## 2. fsext (~3 commits)

### Relevant

- `79b2d619` — feat(fsext): stop upward lookup at a boundary directory
  (covered in Config doc — used by path discovery scoping)

### Others

- `5331fe25` — add -p flag to dirs command (cosmetic)
- `ce08b220` — feat(dirs): add some styling to the dirs command

**Recommendation: SKIP** — our `fsext` has enough for path discovery.
The boundary feature is only needed if we port the config scoping.

---

## 3. Prompts (~8 commits)

### MERGE (applies to bash-first prompt)

- `fd18c38c` — fix(system-prompt): remove "portuguese" as example
  Dumber models respond in Portuguese even when user prompt is English.
  Remove from examples. **Our coder.md likely has same issue.**
  **Files changed:** `internal/agent/templates/coder.md` or `system_prompt.tpl`

### EVALUATE

- `e64db33e` — feat(prompt): require loading appropriate skills before acting
  (covered in Skills doc)
- `1811bec2` — fix(prompts): tweak file reads to encourage more targeted reads
  (tool-call wording — SKIP for bash-first)

### SKIP (tool-call wording)

- `efb3b033` — feat(prompts): remove long prompt option
- `f546a071` — fix(prompt): remind coder to follow `<git_commits>` format
- `f78baed0` — fix(tools): drastically reduce tool call description lengths
- `70d6927f` — chore(tools/agent): improve and correct tool description
- `7230b48f` — chore(tools/agentic_fetch): improve tool description

**Recommendation:** Only `fd18c38c` is worth merging. The rest is
tool-call-specific prompt content.

---

## 4. Provider-specific (SKIP all)

- `57ae2b44` — fix(bedrock): improve credential detection
- `36f0976c` — fix(bedrock): load credentials for bedrock europe
- `ae20f1f6` — feat: add aws bedrock europe
- `74b84f69` — fix(bedrock): enforce `us-east-1` as region
- `4882cbc0` — feat: add alibaba (singapore) provider
- `f34f890e` — fix(deepseek): fix 400 bad request for deepseek provider
- `ea847f28` — fix: make thinking on/off toggle work for deepseek
- `0490a8a6` — fix(bedrock): honor reasoning_effort for Anthropic-on-Bedrock
- `08c5c7d8` — fix(reasoning): enforce can reason from catwalk
- `0a2de47f` — fix: address thinking on/off toggle for provider
- `c7368b7c` — fix: fix thinking on/off toggle for openai-compat providers
- `c8379c30` — fix(auth): add better atomic refresh for hyper
- `59943a82` — feat: open Hyper auth dialog automatically on unauthorized
- `df3a04d1` — chore: isolate store test
- `841eec8f` — fix(models): fix sorting of hyper

**Recommendation: SKIP unless we add that provider.** If we want Bedrock
support, we'll add it fresh using our own provider registration. These
fixes are after the fact for an existing implementation.

---

## 5. Assisted-by / Attribution (MERGE)

### `24da5094` — fix: properly follow `Assisted-by` header spec
Kernel spec says `Assisted-by: AGENT_NAME:MODEL_VERSION` not `AGENT_NAME via`.
**Files changed:** `internal/agent/agent_run.go` — header generation
**Check:** Our `Generated-with` and `Assisted-by` headers — do they match
the spec? Format should be `Assisted-by: Lenos:model-name`.

---

## 6. Test infra (SKIP)

- `53c9774c` — test: reduce amount of logs print
- `4b3dff30` — test: run migration setup once
- `4e66adae` — test: simplify integration tests, run single provider/model
- `e0a3f4db` — chore: `task fmt`
- `45b1e634` — chore: gofumpt project
- `8468c8c0` — chore(tests): update golden files
- `0485adc4` — chore(tests): update golden files
- `651abb48` — chore(golden): rerecord vhs

**Recommendation: SKIP** — test/formatting noise. Golden files won't match.

---

## Merge plan (other)

1. `fd18c38c` — remove "portuguese" example from prompt
2. `24da5094` — fix Assisted-by header format
3. Maybe port `79b2d619` fsext boundary (covered in Config)

Total diff: ~20 lines across 2 files.
