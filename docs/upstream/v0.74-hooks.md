# Hooks (Medium priority)

> 6 commits. Upstream added a hooks system. We have our own `internal/hooks/`
> which may benefit from their interface changes.

---

## 1. Matcher compilation (MERGE)

### `c84ca2d9` — refactor(hooks): move matcher compilation into the runner
Runner now compiles its own matchers, so a config round-trip can't
silently strip them. Hooks with invalid matcher are dropped with warning
instead of silently matching everything.

**Files changed:** `internal/hooks/runner.go`
**Check:** Our hooks runner — where does it compile matchers? If in the
config layer, move to runner.

---

## 2. Env propagation (MERGE)

### `3b3f992f` — feat(hooks): propagate CRUSH/AGENT env vars to builtin shell
Share `CRUSH=1`, `AGENT=crush`, `AI_AGENT=crush` between bash tool and
hook runner so the two surfaces can't drift.

**Files changed:** `internal/hooks/runner.go` — shell setup
**Check:** Our hooks shell — does it get `LENOS=1`, `AGENT=lenos` etc.?
Ensure consistency with our `exec.go` env setup.

---

## 3. Runner interface (EVALUATE)

### `22d6f44e` — feat(hooks): run via shell.Run instead of sh -c
Moves hooks from raw `/bin/sh -c` to the same shell.Run used by bash tool
(shared env, timeout, ctx handling).

**Files changed:** `internal/hooks/shell.go` or `runner.go`
**Check:** Our hooks — do they use `/bin/bash -c` directly or our
`LocalRunner.Run`? Should use the same path for consistency.

---

## 4. Skip (tool-specific)

### `dc003bf7` — feat: PreToolUse hook (#2598)
**Recommendation: SKIP** — PreToolUse is tool-call specific.
Our hooks have PreCmd / PostCmd phases instead.

### `af002dca` — fix(shell): ctx-aware jq builtin
**Recommendation: SKIP** — jq builtin not in our codebase.

### `b9fdf72e` — chore(hooks,skills): update crush-hooks skill
**Recommendation: SKIP** — skill content differs.

---

## Merge plan

1. `c84ca2d9` — matcher compilation in runner
2. `3b3f992f` — env var propagation
3. `22d6f44e` — shared shell.Run path (if applicable)

Total diff: ~60 lines across 2 files.
