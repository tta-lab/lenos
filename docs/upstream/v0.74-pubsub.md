# Pubsub (Medium priority)

> 9 commits, ~4 relevant after excluding server/multi-client specifics.

---

## 1. Buffer fix (MERGE)

### `0585f498` — fix(pubsub): raise default per-subscriber buffer (64→4096)
Streaming turn drops events when TUI re-render stalls.
Default buffer of 64 fills before drain catches up — non-blocking publish
in broker.go silently drops the rest.
Raise to 4096. Also drops dead `maxEvents` field (set but never read).

**Files changed:** `internal/pubsub/broker.go`
**Check:** Our pubsub broker — is buffer size configurable? What's current
default? Same issue likely exists. 64→4096 is safe (a few MB).

**Patch outline:**
```go
const defaultBufferSize = 4096  // was 64
```
Remove `maxEvents` field if present.

---

### `abeaff09` — fix(pubsub): respect channelBufferSize parameter in Subscribe
`NewBrokerWithOptions` accepted `channelBufferSize` parameter, but never
stored or used it — always hardcoded to 64.

**Files changed:** `internal/pubsub/broker.go`
**Check:** Our `NewBrokerWithOptions` — does it store `channelBufferSize`
and pass to `Subscribe`?

---

## 2. Diagnostic logging (MERGE)

### `b223e24e` — feat(logs): add a log line for dropped events
When the broker drops an event because the subscriber buffer is full,
log it.

**Files changed:** `internal/pubsub/broker.go` — the non-blocking send
**Check:** Our non-blocking send — does it log on drop? If not, add
`slog.Warn` or `slog.Debug`.

---

## 3. Server-specific (SKIP)

- `d9acf860` — feat(server): broadcast config changes to connected clients
- `d0254039` — fix(server): display available skills in client
- `0bbc8a87` — fix(ui): restore pills to-do in client/server mode
- `e14d47a8` — fix(noninteractive): crush run reliability (server mode)

**Recommendation: SKIP** — client-server architecture removed.

---

## Merge plan

1. `0585f498` — raise buffer to 4096, drop maxEvents
2. `abeaff09` — fix channelBufferSize storage
3. `b223e24e` — add dropped-event logging

Total diff: ~20 lines across 1 file.
