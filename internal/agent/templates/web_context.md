---
order: 12
optional: true
---
Read available web tool documentation.
```bash
echo "------web --help------" && web --help && echo "------web search --help------" && web search --help && echo "------web fetch --help------" && web fetch --help && echo "------web docs --help------" && web docs --help && echo "------web sgraph --help------" && web sgraph --help
```
